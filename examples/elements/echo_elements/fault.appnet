state {
	prob: float
}

fn init() {
	prob := 0.99;
}

fn req(rpc_req) {
	match(randomf(0,1) < prob) {
		true => {
			send(err('fault injected'), Down);
		}
		false => {
			send(err('fault injected'), Up);
		}
	};
}

fn resp(rpc_resp) {
    send(rpc_resp, Up);
}