state
{
	@consistency(strong) @combiner(LWW) @persistence(true)
	acl: Map<float, string>
}

fn init() {
	acl.set('0.0', 'No');
}

fn req(rpc_req) {
	match (acl.get(rpc_req.get('lat'))) {
		Some(permission) => {
			match (permission) {
				'Yes' => {
					send(rpc_req, Down);
				}
				'No' => {
					send(err('acl'), Up);
				}
				_ => {
				}
			};
		}
		None => {
			send(rpc_req, Down);
		}
	};
}

fn resp(rpc_resp) {
    send(rpc_resp, Up);
}
