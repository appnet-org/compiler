internal
{
    @consistency(strong) @combiner(sum) @persistence(true)
	dummystate: Map<string, string>

    @consistency(strong) @combiner(sum) @persistence(true)
	dummystate2_client_replica: Map<string, string>
}

fn init() {
}

fn req(rpc_req) {
    result := dummystate.get(rpc_req.get('body'));
    result2 := dummystate2_client_replica.get(rpc_req.get('body'));
    send(rpc_req, NET);
}

fn resp(rpc_resp) {
    send(rpc_resp, APP);
}
